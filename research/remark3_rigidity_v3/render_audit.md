# PDF 渲染审计（v3，2026-09-08）

对象：本轮合并后的 `manuscript.pdf`，由 XeLaTeX/xdvipdfmx 编译。

## 结构预检

- A4，13 页；
- PDF 1.5；
- 未加密；
- 非扫描件；
- 无 XFA、JavaScript 或表单；
- 字体已嵌入；
- 文本层可由 `pdftotext` 正常提取中文和数学上下文。

## 双渲染器验证

使用 PDFium 和 Poppler/pdftoppm 分别以 150 dpi 渲染全部 13 页，并按页号逐一比较。结果：13/13 页像素差为 0。在 180 dpi 联系表上逐页人工检查，未发现：

- 公式或正文被页面边界裁切；
- 标题、页眉、页码重叠；
- 黑块、空白字形或缺字；
- 表达式编号越界；
- 参考文献超出右边界。

## LaTeX 日志

最终编译成功，没有 `Overfull \\hbox`。仅参考文献第一项附近有一个 `Underfull \\hbox (badness 2261)`，表示该行字间伸缩偏松；实际渲染没有越界或重叠，因此归类为低风险排版提示，而非 PDF 渲染错误。

## 页面布局观察

- 第 1 页标题区留白较多，是 `\\maketitle` 的常规版式，不影响可读性。
- 新增三个推论后正文由 v2 的 12 页增长为 13 页。
- 第 13 页主要为参考文献，底部留白明显。若强行压回 12 页需要缩小字号、边距或公式间距，会降低论文可读性；当前保留 13 页。

## 结论

当前 PDF 在两个独立渲染器下稳定，未发现需要修复的视觉渲染缺陷。后续若改模板或投稿期刊版式，应重新执行“编译 -> PDF 预检 -> 双渲染 -> 联系表人工检查”的完整回归。

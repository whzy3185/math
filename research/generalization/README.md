# Generalization index

Current date: 2026-09-07.
Branch: `research/quadratic-gap-upgrade`.

The research program now has two complementary workstreams.

## 1. Two-step circulants `C_N(1,s)`

Entry:
`circulant_1s/quadratic_gap_20260906/FINAL_THEOREM_PACKAGE_20260907.md`.

Closed results include:

- explicit sub-`sqrt(8)` periodic family for every jump `s>=2`;
- sharp `s^2(8-Rhat_s)->pi^2`;
- even phase-slip boundary layer;
- exact flat threshold `m(N,s)=2 iff N=2s+2`;
- complete `sqrt(8)` threshold classification on `N=3s`.

## 2. Beyond circulants: Cartesian/Clifford products and general Cayley quotients

Entry: `beyond_circulants_20260907/README.md`.

Current analytic results include:

- Clifford Cartesian-product theorem for arbitrary bipartite factors;
- exact-minimum closure for flat signed regular factors;
- explicit spectra for even tori, grids, boxes and cylinders;
- exact minima for hypercubes, `C4`-tori and Hadamard-product families;
- transfer of all `C_N(1,s)` theorems to unit-generated circulants;
- square-lattice quotient representation of every connected two-generator
  cyclic Cayley graph.

## Next genuine generalizations

The highest-value next targets are:

1. **non-unit two-generator circulants** via skew lattice quotients;
2. **short cycle resonance `L=5,7,9`** via signed pentagon/heptagon strip
   automata, generalizing the `L=3` triangle theorem;
3. **multi-jump circulants `C_N(1,s,t)`**, where the signed square contains
   three pure channels and three mixed channel pairs;
4. **higher-dimensional abelian Cayley graphs** with magnetic/Clifford flux
   patterns and arithmetic quotient obstructions.

Evidence labels and novelty boundaries remain explicit in each workstream.

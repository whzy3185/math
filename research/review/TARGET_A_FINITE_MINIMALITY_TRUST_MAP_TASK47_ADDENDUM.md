# Target A Finite-Minimality Trust Map: Task 47 Addendum

Date: 2026-08-22

Status: **ORDER-22 INDEPENDENT RECORD AND SPECTRAL ROUTES PASS**

This addendum supplements, but deliberately does not modify,
`TARGET_A_FINITE_MINIMALITY_TRUST_MAP.md`. The original trust map is hash-bound
inside the immutable theorem-evidence graph. Task 47 evidence is a descendant
experimental layer and must not silently rewrite that frozen artifact.

| `n` | switching classes | canonical spectral states | primary enumeration | Task 47 independent enumeration | record equality | independent exact decision | residual boundary |
|---:|---:|---:|:---|:---|:---:|:---|:---|
| 22 | 8,388,608 | 97,468 | FKM fixed-weight necklace generator | C full integer-space scan with direct dihedral orbit construction | PASS: 48,734 `Q` records | 97,467 exact Rayleigh exclusions; one exact optimizer; both holonomies; zero uncertified | Per-state vectors are regenerated; source hashes and the ordered certificate digest are archived. |

The independent scan covers all 2,097,152 legal `Q` words and exactly consumes
the complete primary record table with zero missing, duplicate, unconsumed, or
orbit-size-mismatched entries. Full details are in
`TARGET_A_N22_INDEPENDENT_AUDIT.md` and the two machine-readable directories:

```text
research/reproducibility/target_a_n22_independent_record_audit/
research/reproducibility/target_a_n22_independent_spectral_audit/
```

This addendum changes no theorem statement and is not part of the frozen
`bb3c8ac` evidence snapshot unless a later, separately reviewed release elects
to create a new immutable evidence version.

# Referee Checklist

- [x] Gap four is excluded because it is the reference bulk.
- [x] Both lifts and orientations are covered by explicit unitary
  equivalences.
- [x] Every finite vector is evaluated on the full range-two image window.
- [x] The Rayleigh identity uses `H_g=A_g^2` and includes outgoing
  coordinates.
- [x] The six exceptional small gaps are exactly `1,2,3,5,7,8`.
- [x] One fixed vector covers every `g>=9` after three finite local cases.
- [x] The exact comparison threshold is
  `988671163952541/125000000000000`.
- [x] Every witness is strictly above that threshold.
- [x] The minimum margin is the positive gap-eight fraction
  `174815250030533/310875000000000000`.
- [x] The conclusion preserves the strict inequality
  `sup sigma(H_g)>c6+1/250`.
- [x] Equality and rank two at G6 are imported from the separately audited
  global-edge theorem.
- [x] The conclusion is not promoted from single gaps to arbitrary finite
  cores.

## Referee recomputation

Recompute each displayed squared norm and cross-multiply the resulting seven
quotients against the exact threshold. The independent checker performs the
same arithmetic and rejects non-strict or altered records.

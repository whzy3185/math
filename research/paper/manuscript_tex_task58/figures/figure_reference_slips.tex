% TASK58_DRAFT_STUB
\begin{tikzpicture}[
  x=0.72cm,
  y=0.72cm,
  site/.style={circle,draw=black,fill=white,inner sep=1.2pt},
  slip/.style={draw=black,line width=1.2pt},
  reference/.style={draw=black,thin},
  sector/.style={draw=black,dashed,->}
]
  \foreach \i in {0,...,8} {
    \node[site] (p\i) at (\i,1.8) {};
  }
  \foreach \i in {0,...,7} {
    \pgfmathtruncatemacro{\j}{\i+1}
    \draw[reference] (p\i)--(p\j);
  }
  \draw[slip] (p4)--(p5);
  \draw[sector] (3.6,1.2)--(4.9,1.2);

  \foreach \x/\k in {0/1,3/2,6/3} {
    \draw[reference] (\x,0) circle (0.82);
    \foreach \i in {1,...,\k} {
      \pgfmathsetmacro{\a}{90+360*(\i-1)/\k}
      \fill[black] ({\x+0.82*cos(\a)},{0.82*sin(\a)}) circle (1.8pt);
    }
  }
\end{tikzpicture}

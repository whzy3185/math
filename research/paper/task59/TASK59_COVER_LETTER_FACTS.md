# Task 59 Cover-Letter Facts

## Mathematical problem

For every even `n >= 8`, the paper asks whether the distinguished twisted
signing of `C_n(1,2)` minimizes adjacency spectral radius among all edge
signings. The paper does not claim to determine the exact minimum at failing
orders or to classify all minimizers.

## Direct prior art

Suvagiya's Conjecture 3 supplies the signed-circulant problem, flux
coordinates, twisted spectral formula, and verification through order 18.
The present paper resolves the conjectured equality at every even order.

## Main classification

The twisted signing is not optimal precisely at `n=32`, `n=40`, and every
even `n>=48`. Equivalently, equality holds at even orders 8 through 30, at
34, 36, 38, and at 42, 44, 46.

## Structural contribution

A period-eight reference phase has squared edge `eta<8`. Its six-gap phase
slip `G_6` supports an isolated top squared eigenvalue `c_6 in (eta,8)` of
multiplicity two and uniquely minimizes the edge among abnormal positive
single gaps. Charge-compatible separated copies and discrete IMS
localization explain eventual failure.

## Proof architecture

The proof separates a human-readable analytic tail from finite closure. The
interface argument proves failure for every even order at least 240. Exact
finite certificates resolve the irregular small orders and bridge 48 through
238, giving the sharp uninterrupted onset at 48.

## Computer-assisted disclosure

The supplement distinguishes producer code, certificate verification,
independently reconstructed objects, and shared symbolic routines. A
machine-readable manifest binds 12 certificate files by SHA-256 across seven
logical families. The complete proof-grade chain and 121 tamper tests run
through one CPU-only command.

## Submission package

The package contains identified and anonymous 37-page manuscripts plus
identified and anonymous 12-page supplements. All four compile without
warnings. Author metadata and an immutable archive identifier remain to be
inserted before formal submission.

# Complete theorem ledger — 2026-09-14

Branch: `paper/circulant-periodic-gap-20260909`

This ledger supersedes every earlier Paper I ledger.

## 1. Exact fixed-period geometry classification

For the full even-separation reflection-chiral two-defect family, write

\[
L=2(N+m),\qquad h=2m,
\qquad p=2L=4(N+m).
\]

Then the unique full-Bloch gap optimizer is known for **every admissible period**.

### Periods divisible by eight

If

\[
p=8r,
\]

then

\[
\boxed{N=m=r,\qquad h=p/4}
\]

is uniquely optimal for every `r>=1`.

Primary theorem: `ALL_FIXED_PERIOD_BALANCED_OPTIMALITY.md`.

### Periods congruent to four modulo eight

If

\[
p=8r+4,
\]

then

\[
\boxed{
1\le r\le5:\quad(N,m)=(r+1,r),
}
\]

whereas

\[
\boxed{
r\ge6:\quad(N,m)=(r,r+1).
}
\]

Thus there is exactly one finite orientation transition, between

\[
p=44
\quad\text{and}\quad
p=52.
\]

Primary theorem: `ALL_PERIODS_DIVISIBLE_BY_FOUR_OPTIMAL_GEOMETRY.md`.

Exact finite certificates:

- `EXACT_ODD_TOTAL_ORIENTATION_R1_R5.md`;
- `EXACT_ODD_TOTAL_ORIENTATION_R6_R8.md`;
- `verify_exact_odd_total_orientation_switch.py`.

Analytic tail:

- `ODD_TOTAL_EFFECTIVE_TAIL_R9.md` proves `(r,r+1)` for every `r>=9`.

Therefore:

\[
\boxed{
\text{the fixed-period geometry problem is completely solved for every }4\mid p.
}
\]

---

## 2. Exact threshold phase diagram

For every `N,m>=1` and every compatible odd multiplier,

\[
\boxed{R_{N,m,q}<8\iff2m<T_N(3).}
\]

The largest safe defect parameter is

\[
M_N=\frac{T_N(3)-1}{2}
=1,8,49,288,1681,9800,\ldots
\]

and satisfies

\[
\frac{M_N(M_N+1)}2=U_{N-1}(3)^2.
\]

Thus the exact boundary is the square-triangular Pell orbit generated by

\[
3+2\sqrt2.
\]

Primary files:

- `EXACT_GENERAL_SEPARATION_PHASE_DIAGRAM.md`;
- `PELL_SQUARE_TRIANGULAR_PHASE_BOUNDARY.md`.

---

## 3. Exact arbitrary-energy Bloch reduction

The complete characteristic polynomial reduces to scalar Chebyshev data.

Primary files:

- `GENERAL_SEPARATION_ALL_ENERGY_CHARACTERISTIC.md`;
- `ALL_ENERGY_SINGLE_SQUARE_IDENTITY.md`.

The central identity is

\[
P(y;d,e)
=4\left[Z^2-1+(d-2)U_{m-1}(a)^2\right]+(2-e),
\]

with

\[
x=\frac{y-d-4}{2},\qquad a=\frac{y+d-4}{2},
\]

\[
Z=T_N(x)T_m(a)+(ax-3)U_{N-1}(x)U_{m-1}(a).
\]

The orientation-duality identity is

\[
\boxed{
P_{N,m}(y;d,e)-P_{m,N}(y;-d,e)
=4[(d+2)U_{N-1}(x)^2-(2-d)U_{m-1}(a)^2].
}
\]

Primary file: `ORIENTATION_DUALITY_CHARACTERISTIC_IDENTITY.md`.

---

## 4. Minimal `2`-adic canonical phase

If

\[
2^k\Vert s,\qquad k\ge2,
\]

then the shortest compatible primitive period is

\[
p_{\min}=2^{k+1},
\]

and the unique optimizer at that period is

\[
\boxed{h=p_{\min}/4.}
\]

This is now a direct corollary of the stronger fixed-period theorem.

Primary files:

- `ALL_LAYER_MINIMAL_PERIOD_QUARTER_OPTIMALITY.md`;
- `CANONICAL_QUARTER_PERIOD_2ADIC_PHASE.md`.

---

## 5. Macroscopic full-Bloch law and all-orders expansions

If

\[
h/L\to\alpha\in(0,1),
\]

then

\[
\boxed{
L^2\Gamma
\to
\frac{\pi^2}{\max\{\alpha,1-\alpha\}^2}.
}
\]

Balanced geometry uniquely maximizes the leading constant:

\[
p^2\Gamma\to16\pi^2.
\]

There are two universal algebraic series:

\[
A(\ell)=\sum_{j\ge2}a_j\ell^{-j}
\]

for endpoint Robin quantization, and

\[
C(N)=\sum_{j\ge0}c_jN^{-j-4}
\]

for the periodic avoided-crossing gain.

To every algebraic order,

\[
\boxed{
\Gamma\sim A(N)-C(N),\qquad m\le N,
}
\]

and

\[
\boxed{
\Gamma\sim A(m),\qquad m>N.
}
\]

Primary files:

- `ALL_ORDERS_MACROSCOPIC_ROBIN_EXPANSION.md`;
- `ALL_ORDERS_PERIODIC_PHASE_SLIP_EXPANSION.md`;
- `ALL_ORDERS_GLOBAL_GAP_PHASE_DIAGRAM.md`.

---

## 6. Correct physical seam tunneling

Let

\[
n=2q+1,\qquad U_N=U_{N-1}(3).
\]

On the `m>N` side, the global well is compressed-antiperiodic. The best exact roots of `z^n=-1` are

\[
z_0=e^{\pm i\pi/n}.
\]

The first odd-multiplier-dependent gap correction is

\[
\boxed{
 e^-_{N,m}-\Gamma_{N,m,q}
=
\frac{(2+2\cos(\pi/n))\pi^2}
{64\sqrt2\,U_Nm^3}
(1+O(m^{-1}))
+O(U_N^{-2}m^{-6}).
}
\]

The optimizing compressed displacement is

\[
\boxed{
\delta_*
=-\frac{\pi^2\sin(\pi/n)}
{64\sqrt2\,n\,U_Nm^3}
(1+O(m^{-1}))
+O(U_N^{-2}m^{-6}).
}
\]

Thus the correct beyond-all-orders scale is

\[
\boxed{U_N^{-1}m^{-3}\asymp(3+2\sqrt2)^{-N}m^{-3}.}
\]

Primary file: `ANTIPERIODIC_SEAM_TUNNELING_ASYMPTOTIC.md`.

Historical claims of exact physical `z=-1` locking for arbitrary odd multiplier and the intermediate scale `(3+2sqrt2)^(-2N)` are retracted.

---

## 7. Quantitative stability

For `p=8r`, the unique runner-up is eventually

\[
(r-1,r+1),
\]

and

\[
\boxed{
\Gamma^{(1)}_r-\Gamma^{(2)}_r
\sim\frac{\pi^2}{2r^3}
=\frac{256\pi^2}{p^3}.
}
\]

For `p=8r+4`, the nearest-orientation splitting is

\[
\boxed{
\Gamma_{r,r+1,q}-\Gamma_{r+1,r,q}
\sim\frac{\pi^2}{32r^4},
}
\]

while moving farther from balance costs `Theta(r^-3)`.

Primary files:

- `SECOND_BEST_GEOMETRY_AND_STABILITY_GAP.md`;
- `ODD_TOTAL_EVENTUAL_OPTIMAL_ORIENTATION.md`.

---

## 8. Current boundary of the paper

The even-separation reflection-chiral two-defect family is now solved at three levels:

1. **threshold:** exact Chebyshev/Pell classification;
2. **fixed period:** exact unique optimizer for every `4|p`;
3. **large period:** full all-orders Robin/cusp/tunneling expansions.

The next genuine strengthening cannot come from further optimizing separation inside this family.  It must enlarge the variational class, for example to reflection-chiral words with more than two flux defects, or to the complete periodic flux class at a fixed period.
# Theorem ledger — 2026-09-10 exact-geometry upgrade

Branch: `paper/circulant-periodic-gap-20260909`

This ledger supersedes the 2026-09-09 ledger for Paper I. It separates **headline proved results**, **proved supporting results**, **exact computational certificates**, and **open strengthening targets**.

Paper I remains completely independent of the finite-global-minimization paper. No result here concerns the minimum over all finite signings.

---

# I. Headline proved results

## H1. Complete period-two variational theory

Status: **Proved**.

Among all period-dividing-two Hamilton-gauge words:

- for odd jump `s`, the alternating word is the unique minimizer up to translation and has Bloch edge below `8`;
- for even jump `s`, the optimal period-two edge is exactly `8`.

For odd `s`,

\[
g_s=4\min_\theta(\sin^2\theta+\cos^2(s\theta)),
\]

with a unique critical phase and

\[
s^2g_s\to\pi^2.
\]

Higher-order asymptotic expansions are proved.

Primary file: `PERIOD_TWO_VARIATIONAL_THEORY.md`.

---

## H2. General reflection-chiral two-defect mechanism

Status: **Proved**.

For even coefficient half-period `L` and two positive flux defects at any even separation `h`, the Hamilton lift satisfies

\[
\tau_{h+1-j}=-\tau_j.
\]

For jump

\[
s=L(2q+1),
\]

signed reflection gives fiberwise chirality and an even characteristic polynomial.

Primary files:

- `GENERAL_EVEN_SEPARATION_CHIRALITY.md`;
- `GENERAL_EVEN_SEPARATION_BLOCK_JACOBI_REDUCTION.md`.

---

## H3. Exact compressed period theorem and 2-adic optimality

Status: **Proved**.

For the `h=2` two-defect family and every even `L>=4`, every odd multiple

\[
s=L(2q+1)
\]

has a period-`2L` phase with Bloch edge below `8`.

If

\[
2^k\Vert s,
\]

the shortest period allowed by this reflection-chiral ansatz is exactly

\[
2^{k+1}.
\]

Primary files:

- `GENERAL_TWO_DEFECT_COMPRESSION_THEOREM.md`;
- `TWO_DEFECT_PERIOD_OPTIMALITY.md`;
- `ODD_DIVISOR_PERIOD_GAP_HIERARCHY.md`.

---

## H4. Exact all-parameter defect-geometry phase diagram

Status: **Proved**.

Write

\[
L=2(N+m),\qquad h=2m.
\]

Then for every `N,m>=1` and every odd multiplier,

\[
\boxed{
R_{N,m,q}<8
\iff
2m<T_N(3).
}
\]

If the inequality fails, the single antiperiodic fiber `z=-1` is already super-eight. Equality never occurs by parity.

Primary file:

`EXACT_GENERAL_SEPARATION_PHASE_DIAGRAM.md`.

Supporting algebra:

- `GENERAL_SEPARATION_THRESHOLD_FORMULA.md`;
- `SQUARE_FORM_THRESHOLD_IDENTITY.md`;
- `EXPONENTIAL_ANTIPERIODIC_PHASE_LOCALIZATION.md`;
- `ANTIPERIODIC_ONE_FIBER_DETECTION_PRINCIPLE.md`.

This theorem supersedes the earlier asymptotic-only geometry transition as the main finite-parameter result.

---

## H5. Pell / square-triangular spectral phase boundary

Status: **Proved**.

The largest safe defect parameter at fixed `N` is

\[
M_N=\frac{T_N(3)-1}{2}
=1,8,49,288,1681,9800,\ldots
\]

and satisfies

\[
\boxed{
\frac{M_N(M_N+1)}2=U_{N-1}(3)^2.
}
\]

Equivalently,

\[
(2M_N+1)^2-8U_{N-1}(3)^2=1,
\]

so the exact spectral boundary is the square-triangular Pell orbit generated by

\[
3+2\sqrt2.
\]

Primary file:

`PELL_SQUARE_TRIANGULAR_PHASE_BOUNDARY.md`.

---

## H6. Sharp fixed-period logarithmic geometry cutoff

Status: **Proved**.

For coefficient period `P=4S`, let `N_*(S)` be the smallest safe complementary bulk parameter. Then, with

\[
\Lambda=3+2\sqrt2,
\qquad
k=\lfloor\log_\Lambda P\rfloor,
\]

\[
\boxed{N_*(S)\in\{k,k+1\}.}
\]

Thus

\[
\boxed{
\left|h_{\max}(P)-\left(\frac P2-2\log_\Lambda P\right)\right|<2.
}
\]

Only

\[
\log_\Lambda P+O(1)
\]

of the linearly many even separations are unsafe.

Primary files:

- `LOGARITHMIC_BULK_WIDTH_PHASE_BOUNDARY.md`;
- `TWO_INTEGER_LOGARITHMIC_CUTOFF.md`;
- `SPARSE_UNSAFE_SEPARATIONS_AT_FIXED_PERIOD.md`.

---

## H7. Sharp Robin endpoint constants for fixed defect separation

Status: **Proved**.

For fixed `h=2m`,

\[
\boxed{
L^2e_{L,2m}
\to
4\arccos\left(\frac1{T_m(3)}\right)^2.
}
\]

The constants strictly increase to `pi^2`, with

\[
\pi^2-C_m
=8\pi(3+2\sqrt2)^{-m}
-16(3+2\sqrt2)^{-2m}
+O((3+2\sqrt2)^{-3m}).
\]

Primary files:

- `GENERAL_EVEN_SEPARATION_ENDPOINT_ASYMPTOTIC.md`;
- `SEPARATION_CONSTANT_EXPONENTIAL_LIMIT.md`.

---

## H8. Growing-separation Dirichlet endpoint law

Status: **Proved**.

If

\[
m\to\infty,
\qquad
N=(L-h)/2\to\infty,
\]

then

\[
\boxed{N^2e_{L,h}\to\pi^2/4.}
\]

If `h/L -> alpha<1`,

\[
L^2e_{L,h}\to\frac{\pi^2}{(1-\alpha)^2}.
\]

Primary file:

`GROWING_EVEN_SEPARATION_ENDPOINT_THEOREM.md`.

---

## H9. Antiperiodic Dirichlet gap law

Status: **Proved**.

If `N,m->infinity` with `m/N` tending to a positive finite constant, then

\[
\boxed{m^2e^-_{N,m}\to\pi^2/4,}
\]

where

\[
e^-_{N,m}=8-\rho(H(-1))^2.
\]

If `h/L -> alpha in (0,1)`, then

\[
L^2e^-_{N,m}\to\frac{\pi^2}{\alpha^2}.
\]

Primary file:

`ANTIPERIODIC_DIRICHLET_GAP_LAW.md`.

---

## H10. Exact macroscopic full-Bloch gap law

Status: **Proved**.

If

\[
\frac hL\to\alpha\in(0,1),
\]

with both arcs macroscopic, then uniformly over arbitrary odd multipliers,

\[
\boxed{
L^2(8-R_{N,m,q})
\to
\frac{\pi^2}{\max\{\alpha,1-\alpha\}^2}.
}
\]

The leading constant is uniquely maximized by balanced geometry

\[
\boxed{\alpha=1/2,}
\]

where it equals

\[
\boxed{4\pi^2.}
\]

Primary file:

`MACROSCOPIC_FULL_BLOCH_GAP_LAW.md`.

Supporting local transfer lemma:

`HARD_SOFT_DIRICHLET_TRANSFER_LEMMA.md`.

---

# II. Important proved supporting results

## S1. Exact two-phase characteristic equation for `h=2`

Status: **Proved**.

The full growing Bloch determinant reduces to two Chebyshev factors and phase variables `(d,e)`.

File: `GENERAL_TWO_PHASE_CHARACTERISTIC_EQUATION.md`.

## S2. Exact general threshold formula

Status: **Proved**.

For arbitrary even separation,

\[
P(8,z)=\mathcal F_{N,m}(d)-e,
\]

with an explicit four-term nonnegative/single-negative formula.

File: `GENERAL_SEPARATION_THRESHOLD_FORMULA.md`.

## S3. Single-square threshold identity

Status: **Proved**.

\[
\mathcal F(d)-2
=4Z_{N,m}(d)^2-8(x-1)U_{m-1}(y)^2-4.
\]

File: `SQUARE_FORM_THRESHOLD_IDENTITY.md`.

## S4. Exponential phase localization

Status: **Proved**.

Any possible threshold failure for `N>=2` lies in

\[
0\le d+2<U_{N-1}(3)^{-2}.
\]

File: `EXPONENTIAL_ANTIPERIODIC_PHASE_LOCALIZATION.md`.

## S5. Multiplier-uniform eventual endpoint locking for `h=2`

Status: **Proved**.

There is an absolute `L_0` such that the `h=2` compressed family is exactly maximized at `z=1` for all larger even `L` and every odd multiplier.

File: `UNIFORM_EVENTUAL_ENDPOINT_LOCKING.md`.

## S6. Spectral-moment endpoint hierarchy for `h=2`

Status: **Proved**.

Exact endpoint dominance is known for moments `2,4,6`, and for every fixed even moment eventually in `L`.

Files:

- `SPECTRAL_MASS_ENDPOINT_THEOREM.md`;
- `FOURTH_SPECTRAL_MOMENT_THEOREM.md`;
- `SIXTH_SPECTRAL_MOMENT_THEOREM.md`;
- `ALL_FIXED_MOMENTS_ENDPOINT_DOMINANCE.md`.

---

# III. Exact certificate results

## C1. `N=1,2` exact finite diagrams

Status: **Proved with exact Bernstein certificates**.

\[
N=1:\ m\le1,
\qquad
N=2:\ m\le8.
\]

File: `EXACT_SMALL_BULK_PHASE_DIAGRAM_N1_N2.md`.

## C2. `N=3` exact finite diagram

Status: **Proved with exact rational Bernstein certificates**.

\[
N=3:\ m\le49.
\]

Files:

- `EXACT_SMALL_BULK_PHASE_DIAGRAM_N3.md`;
- `verify_exact_N3_bernstein.py`.

These are now historical audit layers because H4 gives the general analytic classification.

---

# IV. Older results retained but no longer headline existence theorems

## O1. Fixed period-eight class for `s=2 mod 4`

Exact edge

\[
4+\sqrt{10+2\sqrt5}<8.
\]

## O2. Fixed period-eight class for `s=4 mod 8`

Strict uniform gap below the same reference edge.

## O3. Original period-`4s` even antipodal family

Still mathematically useful for its distinct sharp large-jump law

\[
s^2g_s\to\pi^2
\]

and its phase-slip asymptotics, but it is no longer the best existence/period construction.

---

# V. Current open strengthening targets

## Q1. Finite-period optimal defect separation

For a fixed finite coefficient period, determine exactly which safe even separation maximizes the full Bloch gap, not merely the sub-eight property.

Numerics suggest a balanced or near-balanced geometry, but no finite exact theorem is currently claimed.

## Q2. Higher-order macroscopic geometry correction

For

\[
h/L\to\alpha,
\]

determine the next term beyond

\[
\frac{\pi^2}{L^2\max\{\alpha,1-\alpha\}^2}.
\]

The balanced case is especially interesting because the two endpoint Dirichlet obstructions are degenerate at leading order.

## Q3. Critical logarithmic geometry window

When the complementary bulk length is itself at the Pell threshold scale

\[
N\sim\log_{3+2\sqrt2}P,
\]

determine the spectral gap, not only whether it is positive.

This regime should interpolate between the macroscopic Dirichlet law and the exact Pell transition.

## Q4. Periodic-sector variational optimality beyond two defects

Determine whether two-defect phases are optimal within a natural class of reflection-chiral words of the same period, or whether more general defect patterns can produce a larger gap.

---

# VI. Current paper-level thesis

The strongest current narrative is no longer merely `2`-adic compression. It is:

> **Periodic signed step circulants exhibit an exactly solvable arithmetic scattering theory. Reflection-chiral two-defect phases admit an exact Chebyshev/Pell phase diagram, logarithmic finite-cell geometry thresholds, and sharp Robin-to-Dirichlet gap laws; in the macroscopic regime balanced defect geometry uniquely maximizes the leading full-Bloch spectral gap.**

This is the theorem hierarchy that should drive the next manuscript rewrite.
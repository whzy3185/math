# Final theorem ledger — 2026-09-14

Branch: `paper/circulant-periodic-gap-20260909`

This file supersedes all earlier theorem ledgers for Paper I.  It records only the current corrected statements.

Paper I is independent of the finite-global-minimization paper: every result below concerns explicit periodic phases and their continuous Bloch spectra.

---

# I. Exact finite geometry theorems

## F1. Exact all-parameter sub-eight phase diagram

Status: **Proved**.

Write

\[
L=2(N+m),\qquad h=2m.
\]

For every `N,m>=1` and every compatible odd multiplier,

\[
\boxed{R_{N,m,q}<8\iff 2m<T_N(3).}
\]

Primary file: `EXACT_GENERAL_SEPARATION_PHASE_DIAGRAM.md`.

The exact safe boundary is

\[
M_N=\frac{T_N(3)-1}{2}=1,8,49,288,1681,9800,\ldots
\]

and

\[
\frac{M_N(M_N+1)}2=U_{N-1}(3)^2.
\]

Thus the boundary is the square-triangular/Pell orbit generated by `3+2sqrt(2)`.

Primary file: `PELL_SQUARE_TRIANGULAR_PHASE_BOUNDARY.md`.

---

## F2. Exact balanced optimizer for every period `p=8r`

Status: **Proved for every integer `r>=1`**.

Among all even-separation reflection-chiral two-defect geometries with

\[
N+m=2r,
\qquad p=8r,
\]

the unique full-Bloch gap maximizer is

\[
\boxed{N=m=r,\qquad h=p/4.}
\]

Primary file: `ALL_FIXED_PERIOD_BALANCED_OPTIMALITY.md`.

Proof:

- exact rational certificates for `2<=r<=8`;
- analytic all-phase theorem for every `r>=9`.

The key universal competitor bound is

\[
\Gamma_{N,m,q}
<2-2\cos\frac{\pi}{2\max\{N,m\}}.
\]

---

## F3. Exact minimal `2`-adic canonical phase

Status: **Proved for every `k>=2`**.

If

\[
2^k\Vert s,
\]

then the shortest compatible reflection-chiral two-defect primitive period is

\[
p_{\min}=2^{k+1},
\]

and the unique gap-maximizing geometry at that period is

\[
\boxed{h=p_{\min}/4=2^{k-1}.}
\]

Primary files:

- `ALL_LAYER_MINIMAL_PERIOD_QUARTER_OPTIMALITY.md`;
- `CANONICAL_QUARTER_PERIOD_2ADIC_PHASE.md`.

---

## F4. Periods `p=8r+4`: exact finite orientation switch through `r=8`

Status: **Proved**.

Here `N+m=2r+1`, so exact balance is impossible.  The two nearest-balanced orientations are

\[
(r+1,r),\qquad(r,r+1).
\]

Exact rational certificates give

\[
\boxed{
1\le r\le5:\quad(r+1,r)\text{ is uniquely optimal},
}
\]

while

\[
\boxed{
6\le r\le8:\quad(r,r+1)\text{ is uniquely optimal}.
}
\]

Primary files:

- `EXACT_ODD_TOTAL_ORIENTATION_R1_R5.md`;
- `EXACT_ODD_TOTAL_ORIENTATION_R6_R8.md`;
- `verify_exact_odd_total_orientation_switch.py`.

The eventual theorem proves `(r,r+1)` is uniquely optimal for all sufficiently large `r`.  The current remaining finite-tail target is to make that theorem effective from `r>=9`, which would close every period `p≡4 mod 8`.

---

# II. Exact all-energy algebra

## A1. General all-energy characteristic equation

Status: **Proved**.

For arbitrary squared energy `y`, the growing Bloch determinant reduces to four scalar Chebyshev quantities and the Bloch coordinates `(d,e)`.

Primary file: `GENERAL_SEPARATION_ALL_ENERGY_CHARACTERISTIC.md`.

The seam coordinate enters only linearly:

\[
P(y;d,e)=\mathcal G(y,d)-e.
\]

## A2. Single-square all-energy identity

Status: **Proved**.

With

\[
x=\frac{y-d-4}{2},\qquad a=\frac{y+d-4}{2},
\]

\[
Z=T_N(x)T_m(a)+(ax-3)U_{N-1}(x)U_{m-1}(a),
\]

one has

\[
\boxed{
P(y;d,e)
=4\left[Z^2-1+(d-2)U_{m-1}(a)^2\right]+(2-e).
}
\]

Primary file: `ALL_ENERGY_SINGLE_SQUARE_IDENTITY.md`.

This identity drives the finite-period variational theory.

---

# III. Macroscopic gap theory

## M1. Full-Bloch leading law

Status: **Proved**.

If

\[
h/L\to\alpha\in(0,1),
\]

then

\[
\boxed{
L^2\Gamma
\to
\frac{\pi^2}{\max\{\alpha,1-\alpha\}^2}.
}
\]

Balanced geometry uniquely maximizes the leading constant:

\[
L^2\Gamma\to4\pi^2,
\qquad
p^2\Gamma\to16\pi^2.
\]

Primary file: `MACROSCOPIC_FULL_BLOCH_GAP_LAW.md`.

## M2. Universal all-orders endpoint Robin series

Status: **Proved**.

There is a universal series

\[
A(\ell)=\sum_{j\ge2}a_j\ell^{-j}
\]

governing both endpoint soft channels to every algebraic order.

Primary file: `ALL_ORDERS_MACROSCOPIC_ROBIN_EXPANSION.md`.

## M3. Universal all-orders periodic cusp series

Status: **Proved**.

At the periodic soft well there is an all-orders phase expansion and cusp-gain series

\[
C(N)=\sum_{j\ge0}c_jN^{-j-4}.
\]

Primary file: `ALL_ORDERS_PERIODIC_PHASE_SLIP_EXPANSION.md`.

## M4. Correct all-orders global algebraic phase diagram

Status: **Proved**.

To every algebraic order,

\[
\boxed{
\Gamma\sim A(N)-C(N),\qquad m\le N,
}
\]

and

\[
\boxed{
\Gamma\sim A(m),\qquad m>N.
}
\]

Primary file: `ALL_ORDERS_GLOBAL_GAP_PHASE_DIAGRAM.md`.

The integer transition occurs between `m=N` and `m=N+1`.

---

# IV. Correct beyond-all-orders seam theory

## T1. Compressed-antiperiodic physical seam correction

Status: **Proved**.

For `m>N`, let

\[
n=2q+1,
\qquad
U_N:=U_{N-1}(3).
\]

The globally selected well is `d≈-2`.  The best exact roots of `z^n=-1` are

\[
z_0=e^{\pm i\pi/n}.
\]

Relative to the convenient physical `z=-1` endpoint gap `e^-_{N,m}`, the true global gap satisfies

\[
\boxed{
 e^-_{N,m}-\Gamma_{N,m,q}
=
\frac{(2+2\cos(\pi/n))\pi^2}
{64\sqrt2\,U_Nm^3}
\left(1+O(m^{-1})\right)
+O(U_N^{-2}m^{-6}).
}
\]

The maximizing compressed displacement from `z_0` is

\[
\boxed{
\delta_*
=-
\frac{\pi^2\sin(\pi/n)}
{64\sqrt2\,n\,U_Nm^3}
\left(1+O(m^{-1})\right)
+O(U_N^{-2}m^{-6}).
}
\]

Thus the correct beyond-all-orders scale is

\[
\boxed{U_N^{-1}m^{-3}\asymp(3+2\sqrt2)^{-N}m^{-3},}
\]

not `(3+2sqrt2)^(-2N)`.

Primary file: `ANTIPERIODIC_SEAM_TUNNELING_ASYMPTOTIC.md`.

`ANTIPERIODIC_EXACT_LOCKING_AND_HIGH_ORDER_GAP.md` has been rewritten to use this correct result.

### Retraction record

Two stronger historical claims are retracted:

1. exact physical `z=-1` locking for arbitrary odd multiplier;
2. the intermediate overstrong scale `O((3+2sqrt2)^(-2N))`.

Neither correction changes any algebraic `1/N` or `1/m` coefficient.

---

# V. Quantitative variational stability

## S1. Runner-up for `p=8r`

Status: **Proved eventually**.

For large `r`, the unique second-best geometry after balanced `(r,r)` is

\[
\boxed{(r-1,r+1).}
\]

The first-versus-second gap obeys

\[
\boxed{
\Delta_r\sim\frac{\pi^2}{2r^3}
=rac{256\pi^2}{p^3}.
}
\]

The first odd-multiplier dependence enters only through the positive tunneling correction of order

\[
U_{r-2}(3)^{-1}(r+1)^{-3}.
\]

Primary file: `SECOND_BEST_GEOMETRY_AND_STABILITY_GAP.md`.

## S2. Odd-total orientation stability

For `p=8r+4`, the nearest-balanced orientation splitting is

\[
\Gamma_{r,r+1,q}-\Gamma_{r+1,r,q}
\sim\frac{\pi^2}{32r^4},
\]

while more distant geometries lose at order `r^-3`. The seam-tunneling correction is exponentially smaller.

Primary file: `ODD_TOTAL_EVENTUAL_OPTIMAL_ORIENTATION.md`.

---

# VI. Current strongest paper-level thesis

The current coherent theorem package is:

> **Reflection-chiral two-defect phases in signed step circulants form an exactly solvable arithmetic scattering family. Their threshold region has an exact Chebyshev/Pell phase diagram and their arbitrary-energy Bloch determinant collapses to a single-square identity. At every period divisible by eight the unique spectral optimizer is the balanced quarter-period geometry; at periods congruent to four modulo eight there is a finite orientation transition already located exactly through `r=8`. The shortest `2`-adic period therefore has a canonical optimizer in every layer. The Bloch edge admits universal all-orders Robin and avoided-crossing expansions, while the first odd-multiplier dependence occurs only at an explicitly computed hard-channel tunneling scale.**

Next primary target: prove the `p=8r+4` compressed-antiperiodic orientation is optimal for every `r>=9`, completing the geometry classification for every period divisible by four.
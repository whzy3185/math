# 核验报告

日期：2026-09-08。运行环境：Python 3.13.5，SymPy 1.14.0。

## 实际执行

命令：`python -m unittest discover -s tests -v`。结果：31项unittest测试全部通过，无失败、无错误。原始终端输出见[test_output.txt](test_output.txt)。

覆盖坐标划分、加权指数分配、相位分离、闭性、因子支撑、非零常数归一化、二维与三维存在/不存在实例、不等权重、一维边界、不可约线性前因子、非初等原函数的梯度、非对称矩阵的转置回代、p=0和奇异矩阵反例。

其中二维单项式测试包含k=0,...,5以及h(t)=t和h(t)=t²+t的12个输入子例；每例遍历全部因子分配，而不是随机抽样。有限子例验证不证明任意k的结论；一般k由文稿的单项式刚性定理证明。

## 文稿构建

执行`make paper`，两个LaTeX引擎均正常退出。英文稿12页，中文稿10页。末次编译无未定义引用或溢出行。PDF已渲染检查；字体使用本机安装字体，未随项目分发字体文件。

## 来源标识

用户上传文件：`2605.09585v1.pdf`（11页）。SHA256：

`71701d71f780be64c1f9b9d2b8a4b8ebf75704ce624f4fb0d9c9043cc43c3894`

原文件只用于本次来源核对，不向公共仓库重新分发。

## 核验不能证明什么

这些测试不是Nevanlinna增长证明，不认证任意维数的数学定理，不证明高次因子的绝对不可约性，不完成全部相关文献比较，也不等于Lean核验或同行评审。数学主结论具有文稿内完整解析证明；该证明的外部审阅状态仍是未完成。

## 仓库交付限制

验证程序的GitHub写入调用被工具拦截；本分支提交论文源码、文献、构建文件和实际测试记录。验证程序及测试源码通过本次交付的完整ZIP提供，不能把仓库中的测试日志误认为仓库已包含这些源码。

# Target A Task 55 Synthesis

## 1. Baseline And Safety

Task 55 began from
`bd1934da29a2eb56cf2045554c00c104d79a7959` on
`agent/target-a-discovery-snapshot`. No branch or worktree was created. The
formal manuscript trees remained frozen at

```text
English  59e3a8f73a152ef06f994e979b7219a3365efeae
Chinese  57ae03fb5b90866f84d0d72b414008678e8f5004.
```

## 2. Mandatory Rank Correction

The decisive hostile finding was an anticommuting G6 symmetry

```text
(Ku)_i=(-1)^i u_(9-i),
K^2=-I,  KA=-AK,  KH=HK.
```

The simple unsquared roots `+sqrt(c6)` and `-sqrt(c6)` therefore give two
orthogonal squared modes at `c6`. Thus

```text
rank P_(H6,{c6})=2.
```

The inherited exact-`r`, codimension-`r`, and problem-specific `r x r`
Feshbach statements were retracted. This correction leaves the independently
proved `N_star=48` theorem intact because its finite rows control the full
matrix and its infinite tail uses only `sup sigma(H6)=c6`.

The infinite tau symmetry is not inferred from sample windows. Period-four
tail representatives plus the finite core prove `Q_(6-i)=Q_i`; the anchor
`tau_7=-tau_0` and two-sided induction prove `tau_(7-i)=-tau_i` for every
integer `i`.

## 3. Corrected Exact-2r Theory

For `r in {1,2,3}` separated G6 interfaces with minimum cyclic distance
`D>=1040`, put

```text
ell=floor((floor(D/4)-12)/8).
```

The independently checked replacement theorem proves exactly `2r` levels in
`[c6-1/400,c6+1/400]`, a codimension-`2r` complement below `c6-1/200`, and

```text
|lambda_j-c6|<3505 r(9/25)^ell.
```

The associated Gram-corrected `2r x 2r` Feshbach determinant is exact in the
fixed window. Exact rational Floquet bounds at all eight cuts give `q=9/25`
and basis condition number below 17. The producer, independent checker, two
mathematical audits, and 29 tamper tests agree.

This yields the sufficient continuous exponential onset

```text
N_exp=3120.
```

It is not an optimal threshold and does not replace the stronger inherited
`N_star=48` theorem.

## 4. Complete Even-Order Classification

Task 55 closed the only remaining orders below the eventual tail. Exact local
interlacing removes every forbidden window; a parity-lifted de Bruijn graph
then enumerates all surviving cyclic words and both holonomy sectors. The
independent checker obtains zero unresolved terminal records at

```text
n=34,36,38,42,44,46.
```

Together with the inherited exhaustive theorem through 32, the exact order-40
LDL witness, and the all-even tail from 48, the final classification is

```text
the conjecture fails exactly at n=32, n=40,
and every even n>=48.
```

This is the strongest Task 55 theorem. It replaces both the earlier bounded
negative search at six orders and the false informal possibility that all
even `n>=32` might fail.

## 5. Multi-Gap Interfaces

A deterministic producer enumerated all 31,008 reflection-canonical primitive
positive compositions with support sum in `{2,6,10,14,18}`. Every record has
an integer finite-support Rayleigh witness strictly above the isolated upper
endpoint for `c6`. An implementation-independent recursive enumerator and a
second alternate checker both pass. The JSONL stream digest is

```text
9c8ef135fc11ca7b8c1761c3d45fb89c65790d97c12f2081787814f046c038bf.
```

The weakest global record is `(3,3)` with quotient `2930/369`. A separate
local lemma proves the stronger arbitrary-length statement that any core
containing consecutive gaps `(3,3)` has a witness at least
`419/53>c6`. Its finite dependency closure includes the two possible
`a=1` numerators, 874 and 902.

The universal finite-core `B0 -> B2` theorem remains open. Inserting or
removing a period-eight reference cell is not a spectral equivalence, so the
bounded enumeration cannot be extended by deleting zero-charge blocks.

## 6. Single-Gap Structure

Task 55 proves an exact quotient involution between the gap-2 and gap-6 Evans
numerators and an exact order-five recurrence for exterior-square observables.
At `c6`, however, the recurrence has complex reciprocal modes of equal
modulus, while the involution exchanges stable and unstable sheets. These
facts explain the previous resultant coincidence but do not prove a physical
all-gap hierarchy. That hierarchy remains open.

## 7. Finite Structural Evidence

Two exact read-only computations were deliberately not promoted.

- The reference-relative graph has 105 states and 164 edges, with a
  420-state/656-edge four-phase lift. `F4` and `F5` have no negative cycle and
  only the reference zero orbit, but no checked artifact or spectral bridge
  exists.
- Periods 25 and 26 contain respectively 337,594 and 649,532 canonical
  primitive orbits, with 58 and 95 survivors; each survivor has an exact
  Rayleigh witness above `c6`. Without a producer/checker closure the
  integrated complete frontier remains `p<=24`.

Both results are `EXACT_FINITE_READ_ONLY`, not computer-assisted theorems.

## 8. Interaction And Limits

The exact `2r` reduction supplies norm bounds, not entrywise asymptotics.
Explicit leading interaction coefficients, physical cluster simplicity,
pairwise additivity, and a genuine three-body coefficient remain open.
Representative 80/120/160-digit transfer/Evans roots are retained only as
high-precision discovery evidence.

The explicit nonzero-residue constructions still prove `limsup<=c6`, and a
dilute-G6 pointed limit gives the matching lower bound for those restricted
families. The minimization over all signings is not controlled. Tight
non-elementary cores, dichotomy, normalized vanishing, and aperiodic limits
remain blockers, so the unrestricted common liminf and common limit are open.

## 9. Evidence Inventory

- `PROVED`: analytic symmetry consequences, corrected localization/min-max
  deductions, abstract simplicity criteria, `(3,3)` local lemma, and the
  restricted pointed-limit argument.
- `COMPUTER_ASSISTED_PROVED`: G6 rank/isolation inputs, exact-`2r` cluster and
  Feshbach theorems, exponential constants and `N_exp=3120`, all-even order
  classification, support-18 multi-gap theorem, and exact symbolic
  single-gap identities.
- `EXACT_FINITE_READ_ONLY`: reference-relative graph and periods 25--26.
- `HIGH_PRECISION`: representative finite-ring transfer/Evans splittings.
- `FALSIFIED`: exact-`r`, codimension-`r`, `r x r` physical Feshbach,
  defective geometric gluing, and raw reference coboundary claims.
- `OPEN`: universal interfaces, physical all-single-gap hierarchy,
  interaction coefficients and simplicity, certified frontier past 24, and
  unrestricted common liminf/limit.

## 10. Publication Boundary

The complete even-order classification and the corrected exact-`2r` theorem
are publication-grade research results once their computer-assisted proof
protocol is presented with the stored checkers and certificates. The bounded
multi-gap theorem is suitable as a rigorous structural appendix. Read-only
frontiers, interaction tables, and common-limit heuristics belong only in a
clearly labeled discussion or future-work section.

No formal manuscript was edited in Task 55.

## 11. Final Status

```text
TARGET_A_TASK55_COMPLETE_EVEN_ORDER_CLASSIFICATION_PROVED
TARGET_A_TASK55_EXACT_2R_CLUSTER_AND_FESHBACH_PROVED
TARGET_A_TASK55_STRUCTURAL_FRONTIER_DEEPENED
```

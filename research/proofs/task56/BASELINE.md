# Target A Task 56 Night Baseline

## Checkpoint

```text
branch: agent/target-a-discovery-snapshot
base:   c26c18f9077f184b8a62684baf2feb1e099edccc
time:   2026-08-24 04:30 Asia/Shanghai
```

The formal English and Chinese manuscript trees remain frozen. Task 56 may
write only research proofs, certificates, scripts, and review artifacts.

## Corrected Mathematical Baseline

The following are inputs, not targets to be reproved from the obsolete Task
54 formulation.

1. A single G6 interface has

   ```text
   rank P_(H6,{c6})=2,
   K^2=-I,  KA=-AK,  KH=HK.
   ```

2. For `r in {1,2,3}` and minimum cyclic distance `D>=1040`, there are
   exactly `2r` squared levels in the fixed near-`c6` window and

   ```text
   |lambda_j-c6|<3505r(9/25)^ell,
   ell=floor((floor(D/4)-12)/8).
   ```

3. The valid finite-dimensional reduction is `2r x 2r`. The former exact-`r`,
   codimension-`r`, and problem-specific `r x r` claims are falsified.

4. The sufficient exponential onset is `N_exp=3120`; the stronger independent
   contiguous explicit-witness threshold remains `N_star=48`.

5. For every even `n>=8`, the conjecture fails exactly at

   ```text
   n=32, n=40, and every even n>=48.
   ```

6. Every one of the 31,008 canonical primitive multi-gap cores with support
   sum in `{2,6,10,14,18}` lies strictly above `c6`. Every arbitrary-length
   core containing consecutive gaps `(3,3)` lies at least at `419/53>c6`.

## Open Lanes

- universal finite-core `B0 -> B2` interface lower theorem;
- physical all-single-gap hierarchy;
- entrywise interaction coefficients, physical simplicity, and genuine
  three-body interaction;
- checked reference-relative graph certificate and a spectral bridge;
- unrestricted common liminf and common nonzero-residue limit;
- certified periodic frontier beyond `p=24` or a structural survivor tail.

## Evidence Gate

A Task 56 theorem requires a complete analytic proof or a deterministic
producer paired with an implementation-independent fail-closed checker.
Exact finite read-only calculations remain `EXACT_FINITE_READ_ONLY`.
Arbitrary-precision roots remain `HIGH_PRECISION_DISCOVERY`. A failed route
must record its obstruction and must not weaken any accepted Task 55 theorem.

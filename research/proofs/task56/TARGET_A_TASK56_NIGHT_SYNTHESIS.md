# Target A Task 56 Night Synthesis

## Corrected Start

Task 56 started from `c26c18f` after Task 55 had replaced exact-`r` by
exact-`2r`, proved the bound `3505r(9/25)^ell`, established `N_exp=3120`, and
classified every even order. No obsolete rank-one or `r x r` dependency was
used. Both formal manuscripts remained frozen.

## Complete Abnormal Single-Gap Theorem

The physical single-gap hierarchy is now closed by a variational argument.
For `g=1,2,3,5,7,8`, six displayed finite-support integer vectors have exact
Rayleigh quotient strictly above the certified upper endpoint for `c6`. A
single vector

```text
(4,0,7,8,8,9,1,7,3,6,1,4,1,2)
```

works for every `g>=9`: only the three local classes `g=9`, `g=10`, and
`g>=11` occur, and the uniform quotient is at least `3094/391=182/23>c6`.
The other tau lift and reflected orientation follow by exact unitary
conjugacies. Together with `sup sigma(H6)=c6`, this proves

```text
sup sigma(H_g)=c6  for g=6,
sup sigma(H_g)>c6  for every positive g not in {4,6}.
```

Gap 4 is the reference bulk and has edge `eta<c6`. An independent integer
checker rebuilds every full output window; 11 fail-closed tests pass.

## Protected Finite-Ring Degeneracy

For the standard one-G6 residue-two ring `n=8k+2`, `alpha=+1`, the cyclic
coefficient word satisfies

```text
Q_(n-6-i)=Q_i,
tau_(n-5-i)=-tau_i.
```

Thus `(K_nu)_i=(-1)^i u_(n-3-i)` obeys

```text
K_n^2=-I,  K_nA_n=-A_nK_n.
```

Every positive squared level has even multiplicity. For `n>=1042`, the Task
55 rank-two window is therefore one distinct squared top level of multiplicity
two, while its unsquared positive and negative partners are simple. This is a
new exact interaction constraint: the two squared modes cannot split in this
symmetry class. It does not extend automatically to multiple interfaces or
the opposite holonomy. A separate hostile audit and fail-closed checker pass.

## Universal Interface Progress

Three boundary-robust local obstruction families were proved.

1. A unit gap gives the uniform quotient `44/5`.
2. Every adjacent pair over `{2,3,5}` is excluded by exact local vectors.
3. Every gap at least 45 is excluded by one fixed vector with quotient
   `18061/2283`.

Together with the Task 55 support-18 and `(3,3)` theorems, any still-possible
primitive competitor must have support at least 22, use the 42-letter alphabet
`{2,3,5,6,...,44}`, and avoid adjacent pairs entirely inside `{2,3,5}`. This
is a strict reduction, not a universal theorem: word length remains unbounded
and reference-cell deletion remains invalid.

## Restricted Liminf Progress

Any sequence eventually containing `(3,3)` has squared spectral liminf at
least `419/53`; a certified support-18 local copy gives at least `2930/369`.
The occurrence may drift around the ring, so these statements close genuine
tight, dichotomy, and normalized-vanishing subclasses. Conversely, the
inherited explicit-family `limsup<=c6` implies that large exact minimizers
eventually avoid all such certified local classes. This sharpens the remaining
blocker but does not prove the unrestricted common liminf.

## Periodic And Reference Lanes

The period-25/26 read-only enumeration was reproduced twice. All 153 survivors
contain cyclic `---`, but the equality period-eight phase contains the same
motif. The result remains `EXACT_FINITE_READ_ONLY`, and the integrated frontier
remains `p<=24`.

The reference-relative graph lane now has a deterministic producer and a
serialized certificate. It reconstructs the 105-state/164-edge base grammar,
the complete 420-state/656-edge phase lift, exact `Q(c6)` Bellman-Ford
potentials for `F4,F5`, and four reference-only zero cyclic SCCs for each
form. The certificate hash is

```text
96e9c3f988deb166bdb1f1eb252baa2cb279313e79b27978762ede8abb394954.
```

This result remains `EXACT_FINITE_PRODUCER`: no independent checker or
graph-cost-to-spectrum bridge was completed, so no spectral theorem is
promoted.

## Final Status

```text
ABNORMAL_SINGLE_GAP_HIERARCHY_PROVED
ONE_G6_FINITE_RING_PROTECTED_DOUBLE_LEVEL_PROVED
UNIVERSAL_INTERFACE_FINITE_ALPHABET_REDUCTION_PROVED
UNRESTRICTED_B0_B2_AND_COMMON_LIMINF_OPEN
```

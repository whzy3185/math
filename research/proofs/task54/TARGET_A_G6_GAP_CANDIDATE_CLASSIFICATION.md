# G6 Upper-Gap Candidate Classification

Exact stable-branch elimination has exactly two roots between the rational
bulk bound `1561/200` and `c6_upper`. One is the Task 50 simple physical root
`c6`. The other lies in

```text
[780868668817504/10^14, 780868668817506/10^14].
```

The unsquared G6 Evans determinant excludes zero on that interval in all four
three-row cofactor charts `012`, `013`, `023`, and `123`; every chart also has
a certified nonzero cofactor pivot. Hence the secondary elimination root is
not a physical G6 level. The resultant supplies completeness only, while the
unsquared condition performs physical selection.

Status: `FULL_G6_UPPER_GAP_CLASSIFIED` / COMPUTER_ASSISTED_PROVED.
